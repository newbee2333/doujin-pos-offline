# Netlify / Cloudflare Pages 响应头配置。
# 用法：放到发布目录根目录、文件名 `_headers`（build-publish.mjs 会自动复制过来）。
#
# 这个应用必须有 COOP/COEP：opfs-sahpool 依赖 SharedArrayBuffer，
# 缺了这两个头会直接显示「当前环境不能营业」——不是降级运行，是拒绝启动。
#
# 注意：本文件的注释只能用 `#`。写成 /* ... */ 会被当成路径规则，
# 真正的头配置反而失效（这个文件之前就是这么坏的）。

/*
  Cross-Origin-Opener-Policy: same-origin
  Cross-Origin-Embedder-Policy: require-corp
  Cross-Origin-Resource-Policy: same-origin

/assets/*
  Cache-Control: public, max-age=31536000, immutable

/index.html
  Cache-Control: no-cache

/sw.js
  Cache-Control: no-cache
