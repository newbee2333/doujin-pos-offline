## 改了什么

<!-- 一两句话说明这次改动的目的，不要逐条复述 diff。 -->

## 为什么这样改

<!-- 背景/触发原因。如果是修 bug，写清复现步骤和根因。 -->

## 验证

- [ ] `npm run typecheck` 通过
- [ ] `npm run test` 通过（38 项）
- [ ] `npm run build` 通过
- [ ] 端到端冒烟通过（`npm run smoke` / `verify-import` / `verify-kiosk-entry`）

<!-- 贴关键输出，例如 "Tests 38 passed" 或冒烟的 OK 列表。 -->

## 影响范围

- [ ] 涉及数据库 schema 变更（若是，说明迁移方案与向后兼容性）
- [ ] 涉及金额/库存计算（若是，说明为什么仍然满足"金额最小单位整数"和"套装按整单合并成分校验"）
- [ ] 涉及 iOS/iPad 兼容性（若是，说明是否在真机验证过）
- [ ] 仅 UI / 文案 / 脚本，无业务逻辑变化

## 未验证或已知遗留

<!-- 明确写出没做到的部分。参照规格第 28 节：未通过真机验收的项必须标注，不能因为"页面能用"就当作完成。 -->
